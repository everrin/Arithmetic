<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.everrin.arithmetic" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ArithmeticActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddTableActivity"
            android:label="@string/title_activity_add_table" >
        </activity>
        <activity
            android:name=".NumberSelectActivity"
            android:label="@string/title_activity_number_select"
            android:theme="@style/Theme.AppCompat.Dialog.Alert" >
        </activity>
        <activity
            android:name=".HistoryDisplayActivity"
            android:label="@string/title_activity_history_display" >
        </activity>
    </application>

</manifest>
